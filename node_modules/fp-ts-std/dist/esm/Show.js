import { flow, pipe } from "fp-ts/function";
export const URI = "Show";
export const contramap = (f) => (m) => ({ show: flow(f, m.show) });
export const Contravariant = {
    URI,
    contramap: (m, f) => pipe(m, contramap(f)),
};
